# Backlog

## Compact rotation-freedom overview

Show a tiny rotation summary for every part, preferably below its dimensions in the parts list. Consider an on-shape label next to the copy count as an alternative. Use concise labels such as `Fixed`, `Half-turns`, `Quarter-turns`, `Free`, or `3 angles`; keep the full angle list in Part properties. This is a backlog item, not implemented.

## Safari popup sizing

The native Safari About and example-picker dialogs stretch vertically, creating oversized gaps, selects and buttons. Reproduce in Safari using the user's September 5 screenshots and correct dialog/content sizing without changing the intended compact layout. Deferred at the user's request during deployment.

## Default demo project

Load the bundled `swim.json` demo project by default instead of an empty project. Keep New project available for starting from scratch. This requests initial loading, not automatic solving. Deferred at the user's request during deployment.

## Default solver auto-termination

Enable solver auto-termination by default instead of a fixed time budget. Wire in the existing support from the sparrow repository, as reported by the user. This is a backlog item, not implemented.
